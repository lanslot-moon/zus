<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.kitona</groupId>
        <artifactId>zus</artifactId>
        <version>${revision}</version>
    </parent>

    <artifactId>zus-starter</artifactId>
    <name>zus-starter</name>
    <description>zus-starter: 启动层(组合根) - Spring Boot 启动入口、配置文件、模块组装，显式依赖 infrastructure 确保 Bean 注入</description>

    <dependencies>
        <dependency>
            <groupId>org.kitona</groupId>
            <artifactId>zus-api</artifactId>
        </dependency>

        <!-- 显式依赖 infrastructure，确保 Repository/Facade 实现被 Spring 扫描 -->
        <dependency>
            <groupId>org.kitona</groupId>
            <artifactId>zus-infrastructure</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring-boot.version}</version>
                <configuration>
                    <mainClass>org.kitona.zus.starter.ZusStarterApplication</mainClass>
                    <skip>true</skip>
                </configuration>
                <executions>
                    <execution>
                        <id>repackage</id>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
